
pub struct MerkleTree {
    leaves: Vec<Vec<u8>>,
    root: Vec<u8>,
}

impl MerkleTree {
    
    pub fn build_tree(leaves: Vec<Vec<u8>>) -> Self {
        let mut current_leaves = leaves;
        for item in current_leaves {

        }        
    }
}